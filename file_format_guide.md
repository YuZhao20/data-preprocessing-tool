# ファイル形式ガイド

このプログラムは、標準的なデータファイル形式を想定していますが、柔軟な読み込み機能により、様々な構造のファイルに対応できます。

## 推奨されるファイル形式

### CSVファイル

**標準形式（推奨）:**
```
列名1,列名2,列名3
値1,値2,値3
値4,値5,値6
```

**特徴:**
- 1行目が列名（ヘッダー）
- 2行目以降がデータ
- カンマ区切り
- UTF-8またはShift-JISエンコーディング

### Excelファイル

**標準形式:**
- 1行目が列名（ヘッダー）
- 2行目以降がデータ
- 最初のシートがデータシート

## 自動検出機能

プログラムは以下の情報を自動的に検出・処理します：

### 1. エンコーディングの自動検出
- UTF-8
- Shift-JIS (CP932)
- EUC-JP
- ISO-2022-JP

### 2. ファイル構造の自動検出
- ヘッダー行の位置
- データ行の開始位置
- 不要な行のスキップ

### 3. データ型の自動識別
- 数値変数
- カテゴリ変数（文字列型）
- カテゴリ変数（整数型：0,1,2など）

## 非標準形式への対応

### ヘッダー行がない場合

```python
df = preprocessor.load_data(
    'data.csv',
    header=None,  # ヘッダーなし
    names=['列1', '列2', '列3']  # 列名を手動で指定
)
```

### ヘッダー行が2行目以降にある場合

```python
df = preprocessor.load_data(
    'data.csv',
    header=2  # 3行目（0-indexed）をヘッダーとして使用
)
```

### 最初の数行をスキップする場合

```python
df = preprocessor.load_data(
    'data.csv',
    skiprows=[0, 1, 2]  # 最初の3行をスキップ
)
```

### インデックス列がある場合

```python
df = preprocessor.load_data(
    'data.csv',
    index_col=0  # 最初の列をインデックスとして使用
)
```

### 特定のシートを読み込む（Excel）

```python
df = preprocessor.load_data(
    'data.xlsx',
    sheet_name='Sheet2'  # 特定のシートを指定
)
```

## よくある問題と解決方法

### 問題1: 文字化けが発生する

**解決方法:**
```python
# エンコーディングを明示的に指定
df = preprocessor.load_data(
    'data.csv',
    encoding='shift_jis'  # Windowsでよく使われる形式
)
```

### 問題2: ヘッダーが正しく認識されない

**解決方法:**
```python
# ヘッダー行を明示的に指定
df = preprocessor.load_data(
    'data.csv',
    header=0,  # 1行目をヘッダーとして使用
    auto_detect_structure=False  # 自動検出を無効化
)
```

### 問題3: 不要な行が含まれている

**解決方法:**
```python
# 特定の行をスキップ
df = preprocessor.load_data(
    'data.csv',
    skiprows=[0, 1]  # 最初の2行をスキップ
)
```

### 問題4: 区切り文字がカンマ以外

**解決方法:**
```python
# 区切り文字を指定
df = preprocessor.load_data(
    'data.csv',
    sep='\t'  # タブ区切りの場合
)
```

## 最低限の要件

プログラムが正常に動作するための最低限の要件：

1. **データが表形式であること**
   - 行と列の構造がある
   - 各行が1つのレコードを表す

2. **列名または列の位置が識別可能であること**
   - ヘッダー行がある、または
   - 列の位置が固定されている

3. **データが一貫していること**
   - 同じ列には同じタイプのデータが含まれる
   - 欠損値は空欄またはNaNで表現される

## データ前処理前の準備

### 推奨される前処理

1. **不要な行・列の削除**
   - メモや説明行を削除
   - 合計行や小計行を削除

2. **列名の整理**
   - 空白や特殊文字を削除
   - 重複する列名を修正

3. **データ型の確認**
   - 数値データが文字列として読み込まれていないか
   - 日付データが適切な形式か

### 自動処理される項目

以下の項目は自動的に処理されます：

- ✅ エンコーディングの検出
- ✅ ヘッダー行の検出
- ✅ データ型の識別（数値/カテゴリ）
- ✅ 欠損値の検出
- ✅ カテゴリ変数の識別（整数型も含む）

## 使用例

### 基本的な読み込み（自動検出）

```python
from data_preprocessor import DataPreprocessor

preprocessor = DataPreprocessor()

# 自動検出で読み込み（推奨）
df = preprocessor.load_data('data.csv')
```

### カスタム設定での読み込み

```python
# 詳細な設定を指定
df = preprocessor.load_data(
    'data.csv',
    encoding='shift_jis',
    header=0,
    skiprows=None,
    index_col=0,
    sep=','
)
```

### 前処理パイプラインでの使用

```python
# 前処理パイプラインでも自動検出が有効
processed_df = preprocessor.preprocess_pipeline(
    file_path='data.csv',
    # その他のパラメータ...
)
```

## トラブルシューティング

読み込みに失敗した場合：

1. **エラーメッセージを確認**
   - エンコーディングの問題か
   - 構造の問題か

2. **ファイルを直接確認**
   - テキストエディタで開いて構造を確認
   - 最初の数行を確認

3. **段階的に設定を調整**
   - まず自動検出を試す
   - 失敗したら手動で設定

4. **サンプルデータでテスト**
   - 小さなサンプルで動作確認
   - 問題が特定できたら全体に適用

