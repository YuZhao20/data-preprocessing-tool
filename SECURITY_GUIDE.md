# セキュリティガイド: GitHubリポジトリの非公開化

## 問題

Streamlit Cloudでデプロイしたアプリから、GitHubリポジトリが特定される可能性があります。

## 対策

### 1. Streamlit Cloudの設定

#### リポジトリリンクを非表示にする

Streamlit Cloudの設定では、リポジトリへの直接リンクを非表示にすることはできませんが、以下の対策が可能です：

1. **Streamlit Cloudのアプリ設定**
   - アプリページの「Settings」→「General」
   - 「Show GitHub link」などのオプションがあれば無効化（現在はこのオプションは存在しません）

2. **アプリのAboutセクションを編集**
   - Streamlit Cloudのアプリページで「Edit app」→「About」
   - GitHubリポジトリへのリンクを削除または非表示

### 2. アプリ内からGitHub情報を削除

アプリのソースコードからGitHub関連の情報を削除します。

### 3. リポジトリをPrivateにする（有料版のみ）

**注意**: Streamlit Cloudの無料版は**Publicリポジトリのみ**対応しています。
Privateリポジトリを使用するには、Streamlit Cloudの有料版が必要です。

### 4. リポジトリ名を変更

リポジトリ名を推測しにくい名前に変更：
- ❌ `data-preprocessing-tool`
- ✅ `ml-tools-2024` や `data-utils-v2` など

### 5. README.mdを最小限に

README.mdに個人情報やGitHubアカウント情報を含めない。

## 実装済みの対策

✅ アプリ内にGitHubへのリンクを追加していない  
✅ README.mdは一般的な内容のみ  
✅ コード内にGitHub関連のコメントを追加していない

## 追加で推奨される対策

### 1. リポジトリの説明を変更

GitHubリポジトリの説明を一般的なものに変更：
- 「データ前処理ツール」→「Machine Learning Utilities」

### 2. リポジトリのトピックを設定

GitHubリポジトリのトピックを設定して、検索結果での表示を調整。

### 3. アプリのメタデータを最小限に

`app.py`のページ設定を最小限に。

## 完全な非公開が必要な場合

### オプション1: Streamlit Cloud有料版

- Privateリポジトリを使用可能
- より高度なセキュリティ設定

### オプション2: 独自サーバーでデプロイ

- VPSや専用サーバーでStreamlitを実行
- 完全な制御が可能
- コストがかかる

### オプション3: リポジトリを分離

- 公開用のリポジトリと開発用のリポジトリを分ける
- 公開用リポジトリには最小限の情報のみ

## 現在の設定確認

現在のアプリには以下の対策が実装されています：

1. ✅ アプリ内にGitHubリンクなし
2. ✅ README.mdは一般的な内容
3. ✅ コード内に個人情報なし

## 注意事項

- Streamlit Cloudの無料版では、Publicリポジトリが必要
- GitHubのPublicリポジトリは誰でも閲覧可能
- 完全な非公開には有料版または独自サーバーが必要

